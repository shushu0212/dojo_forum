<% if user != current_user %>
  <% unless current_user.sent_friend_invitation?(user) || current_user.friended?(user)%>
    <%= link_to 'Add friend', friendships_path(friending_id: user), method: :post, class: "btn btn-secondary"%>
  <% end %>
<% end %>
